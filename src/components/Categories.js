import React from 'react';

const Categories = () => {
  const styles = {
    textAlign: 'center',
    marginTop: '70px',
  };

  return (
    <div>
      <h3 style={styles}>Under construction</h3>
    </div>
  );
};

export default Categories;
